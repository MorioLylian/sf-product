{% extends 'base.html.twig' %}
{% block body %}
<h1>Product detail</h1>
    <hr>
    <p class="text-right">
        <a href="{{ path('product_update', {'id': product.id}) }}" class="btn btn-warning">Edit product</a>
        <a href="{{ path('product_delete', {'id': product.id}) }}" class="btn btn-danger">Delete product</a>
    </p>
    <table class="table table-borderless">
        <tbody>
        <tr>
            <th scope="row">#</th>
            <td>{{ product.id }}</td>
        </tr>
        <tr>
            <th scope="row">Designation</th>
            <td>{{ product.designation }}</td>
        </tr>
        <tr>
            <th scope="row">Reference</th>
            <td>{{ product.reference }}</td>
        </tr>
        <tr>
            <th scope="row">Brand</th>
            <td>{{ product.brand }}</td>
        </tr>
        <tr>
            <th scope="row">Price</th>
            <td>{{ product.price }} €</td>
        </tr>
        <tr>
            <th scope="row">Stock</th>
            <td>{{ product.stock }}</td>
        </tr>
        <tr>
            <th scope="row">Active</th>
            <td>
            	{% if product.active %}
    			<span class="badge badge-success">Yes</span>
    			{% else %}
    			<span class="badge badge-danger">No</span>
    			{% endif %}
            </td>
        </tr>
        <tr>
            <th scope="row">Description</th>
            <td>
                {{ product.description }}
            </td>
        </tr>
        </tbody>
    </table>
   {% endblock %}